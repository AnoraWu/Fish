# setup -------------------------------------------------------------------
rstudioapi::openProject("dirname(getwd)")
# setup -------------------------------------------------------------------
rstudioapi::openProject("dirname(getwd())")
# setup -------------------------------------------------------------------
rstudioapi::openProject(dirname(getwd()))
# setup -------------------------------------------------------------------
if (file.exists("renv/activate.R")) {
source("renv/activate.R")
} else {
stop("renv environment not found. Please initialize renv first.")
}
